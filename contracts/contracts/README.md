# Smart Contracts

The first contract we will build is `DegreeAttestation.sol`.

It will teach:

```text
state variables
structs
mappings
events
modifiers
role checks
degree issuing
degree verification
degree revocation
```

